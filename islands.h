#include <iostream>
using namespace std;
#include <vector>
#include <queue>
#include <string>
#include <math.h>

// declare function to print elements of the region matrix
void print_elements(vector< vector<int> > region);

// declare function to count islands given a certain region matrix
int count_islands(vector< vector<int> > region);